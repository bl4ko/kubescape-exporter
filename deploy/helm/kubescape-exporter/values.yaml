replicaCount: 1

image:
  repository: ghcr.io/bl4ko/kubescape-exporter
  tag: ""
  pullPolicy: IfNotPresent

namespace: kubescape

port: 8080

logLevel: info

priorityClassName: ""

resources:
  requests:
    cpu: 10m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 600Mi

serviceMonitor:
  enabled: true
  interval: 60s
  scrapeTimeout: 30s
  additionalLabels:
    release: monitor

serviceAccount:
  create: true
  name: kubescape-exporter
